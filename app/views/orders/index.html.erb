<table class="table table-bordered">
  <% @orders.each do |e| %>
    <tr>
      <td>
        <%= e.name %>
      </td>
      <td>
      <%= link_to "顯示", order_path(e), :class => "btn btn-primary" %>
      </td>
    </tr>
  <% end %>
  </table>
  <%= paginate @orders %>