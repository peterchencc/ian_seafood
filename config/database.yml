# Add gem "mysql2"
# rake db:create
# rake db:migrate

development:
  adapter: mysql2
  username: root
  password:
  database: inseason-development

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: mysql2
  username: root
  password:
  database: inseason-test
